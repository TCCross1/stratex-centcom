import { ReportPreview } from "./preview/ReportPreviewPage.jsx";

/** One kit for command, property panel, and print. */
export function PacketPreview(props) {
  return <ReportPreview {...props} />;
}

export default PacketPreview;
