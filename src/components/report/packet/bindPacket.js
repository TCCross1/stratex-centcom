/**
 * Single binder. Kit files import from here; domain tests import domains/reports/packet.js.
 * Twin A roofGeometry drives the dimension ticker. Twin B never rules area/pitch/length.
 */
export {
  MISSING,
  bindPacket,
  canMountSkeletonC,
  tickerFromTwinA,
  lintPacket,
  displayCell,
  formatMoney,
  formatNumber,
  formatPct,
  pagesForAudience,
} from "../../../domains/reports/packet.js";

export { default } from "../../../domains/reports/packet.js";
