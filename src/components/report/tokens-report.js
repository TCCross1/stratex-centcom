/**
 * Packet spacing aliases. Every value still reads T — never a second palette.
 */
import T from "../../design/tokens.js";

export const RT = {
  artboardW: T.packet.artboardW,
  artboardH: T.packet.artboardH,
  headerH: T.packet.headerH,
  footerH: T.packet.footerH,
  identityH: T.packet.identityH,
  pad: T.packet.pad,
  gutter: T.packet.gutter,
  innerStroke: T.packet.innerStroke,
  hairline: T.packet.hairline,
  icon: T.packet.icon,
  kicker: T.packet.kicker,
  section: T.packet.section,
  tableHead: T.packet.tableHead,
  tableCell: T.packet.tableCell,
  heroMoney: T.packet.heroMoney,
  radius: T.radius.md,
  radiusLg: T.radius.lg,
  abyss: T.color.abyss,
  textFaint: T.color.textFaint,
  missing: "—",
};

export default RT;
